# WO-6.7 — dispatch status

Append-only trail. Written for the run that has to replace this one, not for a watcher.

- 2026-09-15 — gates: `node tools/wo-gate.mjs WO-6.7` PASS. Depends WO-6.3 ✅, WO-3.26 ✅, WO-4.5 🔨 code-complete (2 open lines both 📆, does not gate). Tree clean. No dispatch files existed.
- 2026-09-15 — route: **Claude Opus, on its own merits.** Deciding signal: the row's one decision is a convention (the readers as the one array three screens draw from) and its Traps are judgment ("the temptation is to make them smart"); also touches the home screen, lifts a mockup stylesheet, carries teacher-facing quiet-panel prose and an open owner's call. Runner-up set aside: Codex — thin wrappers and a harness-checkable count line, but the value is in what the readers refuse to do. Not in the Ship 1 pre-routing table. No probe run (not the Codex route).
- 2026-09-15 — owner's call ("not yet" line on the quiet panel) answered by the dispatcher as **NO, not in this row**, flagged for the owner in the boundary report. Reason found in the tree: `inertRules()` returns rules that are NOT BUILT (`rule.inert`), has returned `[]` since WO-4.4, and knows nothing about term length — wearing `.sig-inert` as shipped draws nothing, and composing the mockup's term-length sentence needs arithmetic no engine produces, which Acceptance line 4 forbids in `src/glance.js`. Reversible: a follow-up adds an engine-side function and a one-line wear.
- 2026-09-15 — claim: `node tools/wo-gate.mjs --start WO-6.7` ran, row reads `🤖 CLAIMED — 2026-09-15`.
- 2026-09-15 — brief written: .claude/dispatch/WO-6.7-brief.md (~19303 bytes, both markers filled). Owner's call answered NO in the brief's routing note.
- 2026-09-15T23:40Z — implementer spawned at Opus (no model override; routed to Claude on merits), handed .claude/dispatch/WO-6.7-brief.md. Awaiting return. Expect 20–40 min; a flat status file, no result file and an unchanged git status for the first 20+ min is a reading implementer, not a dead one.
- 2026-09-15 — **the orchestrator was killed by a session limit after the implementer returned and before `--handoff`.** Seventh dead dispatch here, the fifth killed by a quota, and the first to die at *this* stop: the implementer had finished its mutation round, its revert and its whole doc pass, so the corpse was complete rather than armed or half-built. Recovered from the working tree by the calling session, nothing re-dispatched.
- 2026-09-15 — recovery, commands re-run from the tree rather than read out of `WO-6.7-result.md`: `grep -rn MUTATION src/ tools/ index.html sw.js` — only the eight prose mentions that exist at `HEAD`, no plant surviving · `node tools/wo-sweep.mjs` — `42 · 38 · 0 · 4` · `node tools/wo-gate.mjs --audit` — `PASS` · `node tools/wo-gate.mjs WO-6.7` — `PASS` · `node tools/verify-shell.mjs` — `1373 checks · 1373 passed · 0 failed · 0 skipped`, `42,801 lines · 452s`, `EXIT=0`. **All four reproduce the implementer's cited figures exactly**, which is the WO-5.3 test: a wrong rebuild fails a check rather than matching a total.
- 2026-09-15 — `node tools/wo-gate.mjs --handoff WO-6.7` ran (after `--dry-run`); row reads `🔍 AWAITING VERDICT — 2026-09-15`. One line changed in place, `git ls-files --eol` `lf`/`lf`, no rewrite. **The verifier is owed and has never run.** Dispatch it from a FRESH session and tell it it is a first pass — the six ticked Acceptance boxes and every figure in `WO-6.7-result.md` are the implementer's own claims to CHECK, not findings to confirm.
- 2026-09-15 — two things for the verifier that are the owner's and not the pipeline's. **(1)** The dispatcher answered the row's one `Open` line — the "not yet" `.sig-inert` sentence — as **NO**, and the tree agrees for a reason in `src/signals.js`; owner to confirm or reverse at the verdict. **(2)** `closingIn()` gives grades-due dates, term edges AND IEP/504 review counts **one** horizon, `leadWindowOf()`, default 3 days; the implementer names that as short notice for a legally binding date. Also: `tools/verify/copy-class.mjs` moved for a reason **outside this work order** — a fixture stamp colliding with today's date, WO-1.44's one-date bug in a fourth section — and is not WO-6.7's code.
- 2026-09-15 — **fresh session opened at `🔍 AWAITING VERDICT`, as WO-1.38 intends.** Re-checked before spawning anything: `node tools/wo-gate.mjs WO-6.7` PASS (WO-4.5 still 📆 code-complete, does not gate) · `grep -rn MUTATION src/ index.html tools/` — thirteen hits, every one prose in a comment or in `tools/README.md`, no plant in `src/glance.js`, `src/glance.css`, `src/home.js`, `src/calendar.js` or `src/shell.js`. Nothing re-dispatched, nothing released, nothing ticked.
- 2026-09-15 — verifier spawned at **Opus**, told in as many words it is a **FIRST pass** and that the six ticked boxes and every figure in `WO-6.7-result.md` are claims to check. Awaiting verdict.
- 2026-09-15 — **verdict: PASS.** All six Acceptance lines ✅, no 🙋, no 👤, no 📆. The verifier re-ran everything itself rather than reading the result file: `verify-shell.mjs` `1373 · 1373 · 0 · 0`, `42,801 lines`, `452s`, `EXIT=0`, zero SKIP lines in a 1,523-line log · `wo-sweep.mjs` `42 · 38 · 0 · 4`, all four REVIEWs read individually and none a finding · `--audit` PASS · `WO-6.7` PASS · its own `MUTATION` grep cross-checked against `git grep -c MUTATION HEAD` file by file, identical counts, `src/glance.js` zero. `classCard()` byte-identity proved mechanically: `git show HEAD:src/home.js` vs worktree from `function classCard(` to EOF, **14,704 bytes identical both sides**. The `Open` line's NO answer was tested against `src/signals.js` and upheld (`inertRules()` filters on `rule.inert`; no rule carries one). `copy-class.mjs` judged legitimate fixture repair, not creep. Scope clean — 0 WO-6.8/WO-6.4 panel elements under `#homeView`.
- 2026-09-15 — three things the verifier found that the PASS does not close, carried into the report and NOT ticked away: **(1)** a harness gap — no fixture state has `hits > 0` with week/queue/closing all empty, so a build that dropped `attentionHits()` from the quiet decision would pass all nineteen checks; the code on disk is correct by reading, the harness cannot see it. **(2)** the shared 3-day horizon for IEP/504 reviews becomes real at WO-6.8 and wants an owner's ruling before that dispatch. **(3)** a grades-due date inside its lead window appears in BOTH `weekItems()` and `closingIn()`; WO-6.8 wants that answered engine-side, not by filtering inside a panel.
- 2026-09-15 — maintenance NOT applied. Awaiting the owner's go for `--tick WO-6.7`; `CHANGELOG.md` is the teacher's.
