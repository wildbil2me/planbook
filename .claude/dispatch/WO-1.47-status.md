# WO-1.47 dispatch status

- 2026-09-03 — gates checked: PASS, no dependencies, order WO-1.5 before WO-1.47 satisfied, tree clean, no prior dispatch files.
- 2026-09-03 — route **Claude / Opus** — Traps are judgment (do not delete the rebuild; move the reasoning with the code), produces TESTING.md prose and a known-bugs strike, and one of the five sites is the roster supports panel (accommodation data). Codex set aside on the budget too: 3+ verify-shell runs at ~4.4 min against a 20-minute cap.
- 2026-09-03 — claimed: `--start WO-1.47` ran, row reads 🤖 CLAIMED — 2026-09-03.
- 2026-09-03 — brief written: `.claude/dispatch/WO-1.47-brief.md`, 14320 bytes, both ORCHESTRATOR markers filled and deleted.
- 2026-09-03 — implementer spawned at **Opus** (no model override) with `.claude/dispatch/WO-1.47-brief.md`, awaiting return. Expect 20–40 min; a flat status file and unchanged `git status` are normal for the first 20+ minutes.
- 2026-09-03 — **implementer returned.** Result file `.claude/dispatch/WO-1.47-result.md` (14620 bytes) exists. Its account: rebuild split from `change` to `focusout` at all five sites, one new delegated `focusout` listener in `src/shell.js`, `sw.js` CACHE v107→v108, new harness file `tools/verify/date-zero-key.mjs`, six of seven Acceptance lines claimed closed, the 👤 iPad line correctly left open and not ticked. Claims 1290/1290 verify-shell green, sweep 40·37·0·3, --audit PASS, and a two-direction mutation proof reverted before any doc was written. Independently checked here: `grep -rn MUTATION src/ tools/ sw.js` finds no WO-1.47 plant (all hits pre-existing prose). 14 files touched, staged, uncommitted.
- 2026-09-03 — **handoff written.** `--handoff WO-1.47` ran; row reads 🔍 AWAITING VERDICT — 2026-09-03. This session stops here. The verifier is owed and must be dispatched from a fresh session as a first pass.
- 2026-09-03 — **fresh session, verifier's half.** Row read 🔍 AWAITING VERDICT on arrival, so no routing, no brief, no `--release` (the tool refuses it and names the result file it would orphan). Verifier spawned at **Opus**, told in as many words it is a FIRST pass and that the implementer's result file is a set of claims to check rather than findings to confirm. Awaiting verdict.
- 2026-09-03 — **verifier returned: FAIL.** Line 4's clause "no comment describes a hook it no longer sits on" is violated in `src/shell.js`'s delegated-attribute inventory — lines 75-76, 179-182, 313-315, 335-336 still say the rebuild happens on `change`, and `focusout` appears nowhere in the inventory. The code move itself is correct at all five sites; the harness is real and non-vacuous; all three tools green on the verifier's own runs (1290/1290, sweep 40·37·0·3, --audit PASS); `grep -rn MUTATION` clean. Line 4's box was ticked `[x]` by the implementer and is false. Correction round dispatched to the same implementer role at **Opus**, ❌ quoted verbatim. Row stays 🔍 AWAITING VERDICT — the verdict is still owed and there is no status for being corrected.
- 2026-09-03 — **correction round returned.** Its account: five `src/shell.js` census entries repaired (all four stale + the incomplete `data-support-date`), a new census paragraph putting the `focusout` listener in, the ⚠️ phantom-empty-date note moved onto the store in `editAssignmentField()`, `assigned-and-due.mjs`'s two stale comments and one false `check()` label corrected, `date-zero-key.mjs:136`'s backwards `cloneNode` reasoning corrected, `TESTING.md`'s stated check replaced with one it proved would have caught the failure against `git show :src/shell.js`. Line 4 untick/re-tick claimed. Claims 1290/1290, sweep 40·37·0·3, --audit PASS, no mutation planted. 6 files, +150/-33. Four follow-ups named and left unbuilt. Sent back through the verifier at **Opus** as a full re-pass.
- 2026-09-03 — **verdict: PASS WITH MANUAL CHECKS.** Second verifier pass, full re-pass over all seven lines. Round 1's ❌ repaired and independently proved: the new stated check run against a reconstructed round-1 tree (`git show :src/*.js` into a scratch dir) returns all four stale entries, and the single-line form returns only two — so the `-B3 -A3` window is load-bearing as claimed. Six lines ✅, line 5 (👤) 🙋 and correctly still `- [ ]`, with the static preconditions ruled out first (`focusout` not `blur`; both changed files in `sw.js` SHELL; v108 uncommitted over a v107 HEAD, so one bump covers the set; rebuilt elements keep their delegation hooks). Tools green on the verifier's own runs: 1290/1290 @ 427s, sweep 40·37·0·3, --audit PASS, no MUTATION plant. Two things owed to a human beyond the iPad reading: a `--today` pass over drive 2, and an owner's call on `src/shell.js:585`'s "Three other document-level listeners". Report delivered; awaiting the owner's go before `--tick`.
