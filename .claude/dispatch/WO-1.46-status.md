# WO-1.46 dispatch status

- 2026-09-19 gates: `node tools/wo-gate.mjs WO-1.46` PASS — depends WO-1.44 ✅, order WO-1.5 satisfied, tree clean, no dispatch files.
- 2026-09-19 route: **Claude Opus on merits** — Traps are judgment ("a term boundary is not an event, and the fix may differ"), Acceptance 2 wants a written ruling at each of four lines. Runner-up set aside: budget arithmetic (>=4 harness runs x ~4.4 min = ~17.6 min vs 20-min cap) would have excluded Codex anyway, but the Claude column is read first. No probe run. Pre-routing table does not name this row.
- 2026-09-19 claim: `node tools/wo-gate.mjs --start WO-1.46` ran; row reads 🤖 CLAIMED.
- 2026-09-19 brief written: `.claude/dispatch/WO-1.46-brief.md` (~14.5 KB, both markers filled). Added traps: residue is clock-derived since WO-1.53 so `--today` cannot reach it; line-1 proof is a planted record at the site; derived DAY_OFF must stay before OPENS; weekday-vs-calendar walk; a cousin may stay as-is with the reason at the line.
- 2026-09-19 implementer spawned at Opus (no model override; routed on merits), handed `.claude/dispatch/WO-1.46-brief.md`, awaiting return. Expect 20-40 min; a flat status file, no result file and an unchanged `git status` for the first 20+ minutes is a reading implementer, not a dead one. Do NOT spawn a second implementer on this claim.
- 2026-09-19 implementer returned (57 min, 93 tool uses). Result file `.claude/dispatch/WO-1.46-result.md` exists. Its claims (unverified): twin derived via new `firstClearDayFrom()` in `lib-dates.mjs` with a planted neighbour record + precondition check; `preDropDayFrom()` folded into the helper; three cousins LEFT with reasons at the line; four harness runs 1414/1414 (Sat real clock, --today 09-21/09-23/09-25); one mutation run reddened exactly one check; sweep 42/39/0/3 at 1405 call sites; audit PASS. Five Acceptance boxes ticked by implementer. 9 files modified, +383/-70, `grep MUTATION` clean. Not committed.
- 2026-09-19 handoff written: row reads 🔍 AWAITING VERDICT. Verifier is OWED — new session, `work-order-verifier` at Opus, told it is a first pass. This session stops here.
- 2026-09-20 verifier session (fresh, first pass) entered at 🔍 AWAITING VERDICT. Gate PASS; tree holds the same 9 modified + 3 dispatch files. `grep -rn MUTATION tools/ src/` clean (only standing prose). `node --check` clean over tools/verify/*.mjs. `git diff --numstat` proportionate (+383/-70, no line-ending rewrite). Re-ran `wo-sweep.mjs` → 42 · 39 · 0 · 3, `--audit` → PASS, both matching the result file. Four harness runs (real clock Sun 09-20, --today 09-21/09-23/09-25) started in the background to scratchpad logs; verifier being spawned at Opus in parallel — harness binds port 0 and a mkdtemp profile, so the runs cannot collide.
- 2026-09-20 verifier returned (19.7 min, 49 tool uses): **PASS**, five ✅, no ❌, no 🙋. Own runs: Sun real clock 1414/1414; --today=09-23 1414/1414; own mutation round (`return day;` above the walk, --today=09-21) → 1413/1414 with exactly the derived-date check red, teardown and phase D green; mutation removed by hand, tree confirmed back to the delivered nine files. Sweep re-run 42/39/0/3, audit PASS. Line 4's tick stands on the broader reading (literal reading unreachable by construction since WO-1.53; recommendation: keep the tick with the parenthetical, re-cut is the owner's call). Two stated limits judged limits, not defects. Orchestrator's own runs so far: Sun real 1414/1414 EXIT=0, Mon 09-21 1414/1414 EXIT=0; 09-23 and 09-25 still running. Tree after verifier: same nine unstaged files, nothing staged, `grep MUTATION WO-1.46` empty. No --tick yet — owner has not said go.
- 2026-09-20 orchestrator's own four harness runs complete, all `EXIT=0`, all `1414 checks · 1414 passed · 0 failed · 0 skipped`: Sun real clock (guess 10-01 → 10-02); --today=09-21 Mon (guess Fri 10-02 → Mon 10-05, weekend crossed); --today=09-23 Wed (10-06 → 10-07); --today=09-25 Fri (10-08 → 10-09). Every figure in the result file reproduced independently. Final report written; --tick NOT run — awaiting the owner's go. Next per `wo-gate next`: WO-G2.
