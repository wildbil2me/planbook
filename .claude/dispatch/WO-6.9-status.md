# WO-6.9 dispatch status

- 2026-09-16 gates: `wo-gate.mjs WO-6.9` PASS — depends WO-6.8 ✅, tree clean, no dispatch files.
- 2026-09-16 route: **Claude Opus** on the merits — the review count is the one `supports`-derived item on the glance page, the Traps are a disclosure trap (a review date in an attribute is a disclosure), presentation mode is in the Acceptance. Sensitive surface, ROUTING § Route to Claude, bullet 1. No Codex probe run: the rubric never reached the Codex column. Runner-up set aside: Size S with a mechanically checkable Acceptance reads Codex-shaped, but the work is choosing a landing, not implementing a spec.
- 2026-09-16 open question answered by the dispatcher (the work order says "Answer it when dispatching"): **option 4** — month on today, with the window's far edge named on the calendar when it lies past the drawn month. Reasons: it is the only option right for every lead value (option 1 breaks past a Saturday, not just past a lead of 6; option 2 is the same defect mirrored, and the month grid's borrowed days make both it and today's landing right only by luck of the weekday); it keeps the landing the home button already has; the sentence is about the WINDOW, which is the teacher's setting, and never about a review. Owner may overrule before the verdict.
- 2026-09-16 claim: `wo-gate.mjs --start WO-6.9` ran → `🤖 CLAIMED — 2026-09-16`.
- 2026-09-16 brief: skeleton generated at `.claude/dispatch/WO-6.9-brief.md`; filling markers.
- 2026-09-16 brief: markers filled, no marker left; ~15 KB total (~7 KB added). Spawning implementer at Opus, brief .claude/dispatch/WO-6.9-brief.md, result owed at .claude/dispatch/WO-6.9-result.md. A flat 20+ min stretch from here is a reading implementer, not a dead one.
- 2026-09-16 (later, a new session): **DEAD DISPATCH — power cut.** The orchestrator, implementer and their chain died with the machine somewhere inside the implementer's mutation round. No result file. Recovery in this order: `git status` (11 entries, all the implementer's) → `grep -rn MUTATION` → **one live plant at `src/glance.js:758`, mutation (a), the edge blanked** → reverted to `item.to` → sweep 42·39·0·3 → gate PASS → self-check 40/40 → `verify-shell` 1411/1411 on the repaired tree (matches the implementer's cited figure, taken before the plant) → tree staged → M(a) re-planted, 7 red, reverted → M(b) planted, 7 red, reverted → `TESTING.md § WO-6.9` written (it was cited by `tools/README.md` and did not exist) → `.claude/dispatch/WO-6.9-result.md` written from the tree. Seventh dead dispatch; the first not killed by a quota or an API error. The tick state was five `[x]` over an armed tree, which is the WO-5.1/5.3 shape again — and again the ticks said nothing about whether the code was armed.
- 2026-09-16 handoff: see the line `--handoff` writes below.
- 2026-09-17 owner: **option 4 confirmed, wording left as built.** The open `Open` line is closed by the owner, not the dispatcher. Verifier still owed — from a fresh session.
- 2026-09-17 (verifier session): `git status` first — 13 staged paths, same set the recovery recorded. `wo-gate.mjs WO-6.9` PASS, row `🔍 AWAITING VERDICT — 2026-09-16`. `grep -n MUTATION` over every changed path: no live plant in `src/`, `tools/`, `index.html` (the `src/shell.js` hit is HEAD's own standing comment). No route, no brief, no release — this session is the verifier's. Spawning `work-order-verifier` at Opus, told it is a FIRST pass, the result file's claims are claims to check; awaiting verdict. A flat stretch from here is a reading verifier.
- 2026-09-17 verdict in: **PASS WITH MANUAL CHECKS** (work-order-verifier, Opus, ~19 min). Lines 1-5 ✅ by its own runs (sweep 42·39·0·3; verify-shell `--today=2026-09-16` 1411/1411; real clock 1396·1391·5 with all 5 red in attendance.mjs § WO-2.3, WO-6.9 block 11/11 on both). Line 6 🙋 iPad. Backwards-paging seam pressed and holds. Finding OUTSIDE this tree: `nodeColumns(6,1)` in attendance-passes.mjs:2172 collides with classes-terms.mjs:699's hard-coded 2026-09-09 record every year 09-17..09-24 — third WO-1.44 site, wants its own row. Tick NOT applied; owner's go owed.
- 2026-09-19 owner: **line 6 read on the iPad and closed** — v120, force-quit first, installed test app on the LAN server, lead at 14; landing on September, sentence naming the October edge, week wording, presentation mode and a cold relaunch all as built. `--tick WO-6.9` → ✅ DONE — 2026-09-19. Same sitting closed WO-6.4's two 👤 lines and ticked it; ROADMAP dashboard hand-edited to 8/8 and 71/81, `--audit` PASS. CHANGELOG owed.
