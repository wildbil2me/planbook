# WO-5.2 — Templates · dispatch status

- 2026-08-28 — gates checked: `PASS`, depends WO-5.1 ✅ DONE, tree clean, no dispatch files. Note `next` returns WO-1.27 (row 12); WO-5.2 is row 21 and was named by the owner. Gates clear either way.
- 2026-08-28 — route **Claude Opus**, on its own merits, no probe run and none needed. Deciding signal: ROUTING.md § "Later phases, at a glance" names **all of Phase 5 Claude-only as a property of the work**, and this row is teacher-facing prose (starter templates in suite voice) sitting one import away from the merge-field resolver, a named sensitive surface. Runner-up set aside: the `templates[]` CRUD half reads Codex-shaped — settled schema, mechanically checkable backup round-trip — but the row is not separable and the prose plus the presentation-mode question on the live preview are the whole of its risk.
- 2026-08-28 — **NOT claimed, and not dispatched.** Three `Open` lines on the work order are unanswered; the work order says *"Answer this when cutting the row, not in the editor"* and the drawing stamps two of them **owner's call**. Held for the owner rather than decided here. No `--start` has run, so nothing needs `--release`.
- 2026-08-28 — owner answered all four items: header icon in `hdr-right-controls` opening a full main-area screen · block strip permanent and green when clear · eight starter templates shipped filled in, none auto-loaded · **iPad palette reading waived**, palette ships as drawn, sheet-over-keyboard alternative explicitly not substituted. Checked the waiver's escape hatch before acting on it: shipping as drawn is the structural status quo (the drawing already stacks list · editor · preview · palette at 390px), so skipping the reading leaves comfort unmeasured, not shape undecided. Proceeded.
- 2026-08-28 — claimed: `--start WO-5.2` ran, status now `🤖 CLAIMED — 2026-08-28`. A replacement run is RESUMING, not starting.
- 2026-08-28 — brief written to `.claude/dispatch/WO-5.2-brief.md`, 12762 bytes, both `<!-- ORCHESTRATOR -->` markers filled and deleted. Carries the route, the three answered `Open` lines, the waiver on the record, and the presentation-mode obligation the live preview owes `src/supports.js`.
- 2026-08-28 — implementer spawned at **Opus** (no model override; routed to Claude on its own merits, not a fallback), handed `.claude/dispatch/WO-5.2-brief.md`, awaiting return. Expect 20–40 min; a flat status file and an unchanged `git status` for the first 20+ min is a reading implementer, not a dead one.
- 2026-08-28 — **the orchestrator was killed by an API session limit (HTTP 429) at the handoff between the implementer's return and the verifier's spawn.** The fifth dead dispatch here and the third killed by a quota. **The implementer had COMPLETED**: `WO-5.2-result.md` was written in full, all four Acceptance boxes and `✅ DONE` were already in the tracker, and every command its prose cited was real — but nothing had verified any of it. A cooperative pause requested at the implementer/verifier seam was queued and never read, because the orchestrator got no further tool round. **A pause that depends on the agent taking another turn cannot survive the death of that turn** — the seam it was aimed at is the same seam the quota kills.
- 2026-08-28 — recovered by the main session, not re-dispatched. `grep -rn MUTATION` over the delivered files: **clean** (`src/shell.js:710` matches but `git diff` shows 0 added lines — pre-existing). The tree was **staged before re-running the harness**, because `verify-shell.mjs`'s mutation sections `git checkout` files and would have reverted 876 lines of unstaged work. All four cited commands re-run from the tree and reproduced exactly: `verify-shell.mjs` 1223 · 1223 · 0 · 0 (413s, exit 0) · `wo-sweep.mjs` 34 · 30 · 0 · 4 · `--audit` PASS · `--self-check` PASS 27/27.
- 2026-08-28 — **verifier re-dispatched alone** against the recovered tree, told the boxes were the implementer's own claims rather than findings. Verdict **FAIL on one ❌, and not on an Acceptance line**: `plans/work-orders/phase-5-outreach.md` and `TESTING.md` both recorded the header budget of the attempt that went RED — 41px and `96 → 72` — where the shipped `src/shell.css` is 49px and `96 → 64`. The result file narrates 49 correctly; the two landed documents did not, and the result file also flags a fifth header icon as the next thing someone will size from exactly those numbers. **All four Acceptance lines verified**, including the one most likely to be a round-up — line 1's fixture is discriminating on four numbers (`concern/admin` → 0 is the one a tone-only filter would pass). Presentation-mode obligation verified; the picker is emptied, proven by a `textContent` search that reaches `<option>` text inside a `display: none` row.
- 2026-08-28 — ❌ repaired: four values in two files, `41px` → `49px` and `96 → 72` → `96 → 64`. No code moved; sweep and `--audit` re-run green after the prose edit. Stray zero-byte `_brief.err` unstaged and deleted before it could be committed — the WO-3.26 shape.
- 2026-08-28 — maintenance protocol run by the main session: `CHANGELOG.md` entry added (the implementer's draft, extended to say the header row is now full and that no send path exists yet), and the verifier's residual booked — **WO-5.3 gains a sixth Acceptance line** carrying the "offered separately **at send time**" half that WO-5.2 could not close and that none of WO-5.3's five existing lines re-asked. **Deliberately not an `**Owes**` pointer**: nothing was re-homed and WO-5.2's box closed honestly on the narrower reading, and a pointer must land on a box that is still `[ ]`.
- 2026-08-28 — **owed to a human, unchanged and not closed**: the palette chip under a phone keyboard, owner-waived. No box exists for it and none was ticked. Every 390px reading in this work order is emulated, the 49px header repair included.
