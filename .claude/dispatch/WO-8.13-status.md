# WO-8.13 dispatch status

- 2026-09-20 — gates: `node tools/wo-gate.mjs WO-8.13` PASS; status ⬜, depends on nothing, tree clean, no dispatch files. Work order read in full as it stands after 69422b9 (label selector ruled into Acceptance line 3; `tools/verify/build-line.mjs` named as the harness home).
- 2026-09-20 — route: **Claude Opus, on its own merits** — teacher-facing prose (section label, TESTING.md line, 👤 reading) and judgment Traps (do not restate the licence, no copyright/version line, do not touch the two rows beside it). Runner-up set aside: Codex-shaped mechanics and two harness runs (~9 min) fit the cap, but the Claude column is read first, so this is not a fallback and the tier is Opus. Not in the Ship 1 table; 🎒 row 42 named explicitly by the caller.
- 2026-09-20 — claim: `--start WO-8.13` ran, row reads 🤖 CLAIMED — 2026-09-20.
- 2026-09-20 — brief written: `.claude/dispatch/WO-8.13-brief.md` (~14 KB, both markers filled: placement after the FERPA row, one selector on shell.css:655, check lives in build-line.mjs's existing open, delete-once proof with MUTATION marker, scope fence).
- 2026-09-20 19:46 — implementer spawned at **Opus** (no model override), handed `.claude/dispatch/WO-8.13-brief.md`; awaiting return. Expect 15–40 min; the first 6–20 min are reading and the status file, result file and `git status` all stay flat through it — that is normal, not a hang.
- 2026-09-20 20:12 — implementer returned (Opus, ~27 min, 63 tool uses). Claims: 7 files touched (index.html, src/shell.css, sw.js v126, tools/verify/build-line.mjs +5 checks, TESTING.md, tools/README.md count 1432→1437, phase file lines 1/2/3/4/6 ticked); harness green 1446/1446 then red 1441/1446 with the row deleted, restored; sweep 42·39·0·3. Lines 5 (same commit — not committed) and 7 (👤) left blank. Orchestrator confirmed: result file exists, `git status` names exactly those 7 files, `grep -rn MUTATION` over the four code files finds nothing, `node --check build-line.mjs` clean. Nothing here is verified.
- 2026-09-20 — handoff: `--handoff WO-8.13` ran; row reads 🔍 AWAITING VERDICT. **Verifier is owed, in a fresh session, at Opus, told it is a first pass.** This session stops here.
- 2026-09-20 — verifier dispatched at **Opus**, fresh session, told in as many words it is a FIRST pass and that the implementer's result file holds claims to check, never findings to confirm. Awaiting verdict.
- 2026-09-20 21:31 — verdict: **PASS WITH MANUAL CHECKS** (Opus verifier, ~20 min, 37 tool uses). No ❌. All five ticked lines (1,2,3,4,6) re-derived independently and true. Verifier ran both harnesses itself: verify-shell 1446/1446 exit 0, zero skips; sweep 42·39·0·3. It re-ran the deletion proof in a scratchpad COPY of the tree (not the repo — the work is uncommitted and a git checkout would have destroyed it) and reproduced 1446/1441/5 exit 1 with all five new checks red. grep MUTATION clean. Lines 5 (closes at the commit) and 7 (👤 iPad) correctly open. Two carried findings: the work order Acceptance 3 parenthetical claiming touch-targets.mjs measures the row is FALSE (no .doc-link has ever been measured, for any of the three rows) — proposed follow-up; and the stale OAuth origin comment in index.html is untouched context, not this work order's. Ticking NOT applied — awaiting the owner.
