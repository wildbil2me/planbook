# WO-8.14 — dispatch status

- 2026-09-22 — gates checked: `node tools/wo-gate.mjs WO-8.14` PASS. Status ⬜ NOT STARTED, depends on nothing, tree clean, no dispatch files.
- 2026-09-22 — **route: Claude Opus.** Acceptance line 2 is a mutate·run·revert on `src/shell.css`, which ROUTING § "Route to Codex" excludes by name (a cap-time SIGTERM leaves a broken stylesheet in the tree). Opus, not a Sonnet fallback, because it is Claude-column on its own merits: it writes a `TESTING.md` line that must link back to WO-8.13's correction rather than restate it. Runner-up set aside: the rest reads Codex — one existing sweep widened, a named selector, mechanically checkable. No Codex probe run; the route never reaches it.
- 2026-09-22 — **claimed**: `--start WO-8.14` ran, row now 🤖 CLAIMED — 2026-09-22. (A replacement run reading this is resuming, not starting.)
- 2026-09-22 — **brief written**: `.claude/dispatch/WO-8.14-brief.md` (11.6 KB total; orchestrator additions ~2.2 KB, both under the caps). Both `<!-- ORCHESTRATOR -->` markers filled and deleted.
- 2026-09-22 — **implementer spawned at Opus** (`work-order-implementer`, no model override), handed `.claude/dispatch/WO-8.14-brief.md`. Awaiting return. Expect 20-40 min; a flat status file, an absent result file and an unchanged `git status` are the normal first 20+ min of a reading implementer, not evidence of death. Do not spawn a second one.
- 2026-09-22 — **implementer returned.** Result file written. Its own account (claims, not findings): one new `check()` inside the existing About-modal block of `tools/verify/touch-targets.mjs` naming all three `.doc-link` rows individually; mutation round run three full harness passes (green 1462/1462 → mutated 1461/1462 exit 1 → restored 1462/1462); `src/shell.css` byte-identical to HEAD; `grep -rn MUTATION` clean on the touched files; `tools/README.md` count 1450 → 1451; a new `TESTING.md` § WO-8.14; all five Acceptance boxes ticked by the implementer. One in-scope repair flagged (a `tools/README.md` sentence saying "exactly one `else check(`" became false) and three declined temptations proposed as follow-ups.
- 2026-09-22 — **handoff written**: `--handoff WO-8.14` ran, row now 🔍 AWAITING VERDICT — 2026-09-22. This session stops here. The verifier is owed and is a NEW dispatch from a NEW session (WO-1.38): spawn `work-order-verifier` at Opus against WO-8.14, told in as many words that it is a first pass and that the implementer's self-claims are claims to check, never findings to confirm. Nothing is ticked at the roadmap/dashboard level; `--tick` is the only way out and waits on the verdict.
- 2026-09-22 — **verifier dispatched** (fresh session, WO-1.38): `work-order-verifier` spawned at Opus against WO-8.14, told in as many words that this is a FIRST pass and that every claim in the result file — including all five already-ticked Acceptance boxes — is a claim to check, never a finding to confirm. Awaiting verdict; nothing is ticked.
- 2026-09-22 — **that verifier dispatch died with its orchestrator.** The orchestrator session handed back ~96s after spawning it, before any verdict returned; a nested agent does not survive its parent, so the line above records a dispatch that reported nothing and no verdict exists. Row correctly stays `🔍 AWAITING VERDICT`; nothing ticked, nothing to `--release`. Re-dispatching the verifier as a fresh FIRST pass.
- 2026-09-22 — **correction: the line above is false, and the one before it was right.** That verifier did NOT die with its orchestrator. The orchestrator handed back after ~96s, but its child kept running ~21 minutes and returned a complete first-pass verdict — **PASS, all five Acceptance lines verified**, including a mutation round it ran itself and restored hash-clean. The premise "a nested agent does not survive its parent" was wrong, and acting on it spawned a **second, redundant verifier**, whose mutation is the live `MUTATION WO-8.14 verifier` in `src/shell.css` the first verifier's report flags as a concurrent-session hazard. Read that hazard as self-inflicted by the re-dispatch, not as anything wrong with the delivered tree. The scar: **a short orchestrator duration says the orchestrator stopped, not that its children did** — poll `ListAgents` or the tree before declaring a dispatch dead, and note the standing rule is already "never call a dispatch dead from file mtimes."
