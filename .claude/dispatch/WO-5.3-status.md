# WO-5.3 — Send flow · dispatch status

- 2026-08-28 — gates checked, PASS: WO-5.2 ✅ DONE, no hard-ordering block, tree clean, no dispatch files.
- 2026-08-28 — **route Claude Opus, on the work order's own merits.** Sensitive surface (merge-field resolver `blocked` contract, accommodation fence, audience picker adjacent to `supports.caseManager`) + teacher-facing prose (truncation warning, the honest `mailto:` limit). ROUTING.md § "Later phases" names all of Phase 5 Claude-only as a property of the work. Runner-up set aside: the percent-encoding half is Codex-shaped but inseverable, and the multi-run harness demand (~4.4 min/run) does not fit the 20-min cap. No Codex probe run.
- 2026-08-28 — **claim taken**: `--start WO-5.3` wrote `🤖 CLAIMED — 2026-08-28`.
- 2026-08-28 — **brief written**: `.claude/dispatch/WO-5.3-brief.md`, 13.3 KB, both `<!-- ORCHESTRATOR -->` markers filled and deleted. Added § 2b: six work-order-specific traps (counselor vs caseManager, guardian-array vs AUDIENCES, the contact log belongs to WO-5.4, the mailto ceiling, encoding proof, blocked-draft gate) plus the presentation-mode question and the mutation-proof rule.
- 2026-08-28 — **implementer spawned at Opus**, handed `.claude/dispatch/WO-5.3-brief.md`, awaiting return. Expect 20–40 min; a flat status file, an absent result file and an unchanged `git status` are the normal first-20-minutes shape and are not evidence of a dead run. TodoWrite is disabled this session, so this file is the whole trail.
- 2026-08-28 — **DISPATCH DIED.** API session limit (HTTP 429), killed while the orchestrator was blocked on the implementer's return. **The fifth dead dispatch in this repository and the third killed by a quota** — WO-4.3, WO-5.1, WO-5.3. No result file was ever written.
- 2026-08-28 — **recovery, by the session that dispatched it, from the tree and not from the prose.**
  - **`grep -rn MUTATION` first, per CLAUDE.md § Accommodations and `AGENTS.md`. It found five live mutations** — WO-5.1 left one; this corpse left five. The implementer died mid-mutation-proof: `src/outreach.js` and `src/outreach-view.js` carry mtime 21:11:39, *after* the whole doc pass (20:50–21:11), which is the WO-5.1 shape exactly.
  - Each original was re-derived from surviving prose and from the harness, never guessed:
    - **MUTATION 1** `src/outreach-view.js:300` — `const url = true ? …` → **`ready ?`**. Forced a live `mailto:` URL onto a blocked draft. `verify/outreach.mjs:552` asserts `refused.ready === false && refused.hasHref === false && refused.href === null`, which pins it.
    - **MUTATION 2** `src/outreach.js:234` — the CRLF normalisation was **deleted outright**, leaving `encodeURIComponent(text(value))`. Restored to `text(value).replace(/\r\n|\r|\n/g, '\r\n')`, the exact form the harness round-trips against. This was the dangerous one: it is invisible on screen and produces bare `%0A`, the mangled-paragraph failure this work order's own Traps line exists to catch.
    - **MUTATION 3** `src/outreach-view.js:238` — `templatesFor(doc, '', audience)` → **`templatesFor(doc, tone, audience)`**. The blanked argument is the **tone**, not a class id, so the mutation did precisely what the comment three lines above it forbids: hand a guardian's concern template back for a praise draft. It defeated the seventh Acceptance line while that line sat ticked.
    - **MUTATION 4** `src/outreach-view.js:537` — the two boxes were self-assigned (`field.value = field.value`) instead of emptied. **A presentation-mode disclosure**: a named student's business and a guardian's address left on the glass under a projector, against this file's own "EMPTIED RATHER THAN HIDDEN" ruling.
    - **MUTATION 5** `src/outreach-view.js:91` and `:764` — an `update` import and an `update(function(){})` call in `toggleOutreachCopy`, against the file header's own line 22: *"IT IS NOT A WRITER. Nothing here calls `update()`."*
  - Reverted with the Edit tool rather than `sed`, per the standing line-ending rule. Diffstat after revert is `+774 / +292` against `+776 / +292` before — a two-line delta and no line-ending rewrite. All three files pass `node --check`.
  - **Re-ran every command the prose cites rather than reading its numbers.** `wo-sweep.mjs` 34 · 31 · 0 · 3 green (the three REVIEW rows are the standing pre-existing ones). `wo-gate.mjs --self-check` PASS 27/27. `--audit` PASS. `sw.js` CACHE bump v102→v103 present and both new files in `SHELL`.
  - **What the corpse's numbers are worth:** `tools/README.md` claims `1251 checks · 1251 passed · 0 failed · 0 skipped`, 410s, exit 0, "measured on the delivered tree". It cannot have been — the tree it describes had five mutations in it and would have gone red. **A green harness number in a dead dispatch's prose is a timestamp, not a state.** Re-run below.
  - **The mutation proofs themselves are unrecorded and stay unproved.** Five went in, none came out, and no result file says which turned red. That the checks exist is not that they bite; whoever verifies this owes that round again.
- 2026-08-28 — **harness re-run on the repaired tree, green, and it reproduces the corpse's figures exactly**: `1251 checks · 1251 passed · 0 failed · 0 skipped`, 37,455 lines, 29.9 lines per check, 410s, exit 0. Zero `FAIL` anywhere in the run. **That the recovered tree lands on the same numbers the implementer wrote down is the evidence the five reverts were right** — a wrong reconstruction would have failed a check rather than matched a total, and MUTATION 2's CRLF normalisation was rebuilt from prose and round-trip rather than recovered verbatim.
  - All 28 `verify/outreach.mjs` checks pass, including the four the mutations attacked, each named at its own line: tone+audience (`praise/guardian 3, concern/guardian 1, praise/counselor 0` — Acceptance line 7); the round trip (`6 CRLF pair(s), 0 bare %0A, round trip = true`); presentation mode (`subjectField:"", bodyField:"", name:false, address:false`); and the blocked draft carrying no `href`, with the link restored on correction.
- 2026-08-28 — **the owed mutation-proof round, re-run from scratch by this session.** The five mutations are known exactly, so re-deriving them was cheaper and safer here than handing the job on. `verify-shell.mjs` takes no section filter, so each proof is a full 410s run, driven one at a time with the tree **staged first** — a mutation revert restores from the INDEX, and the reverts were unstaged (the standing "mutation tests clobber unstaged edits" rule). Each mutation asserts its needle is found **exactly once** before any run, per `verify/keys-legend-guards.mjs`'s own rule that a `replace()` whose needle has moved is a no-op proving nothing.
  - **Result: three of the five bite, and the two that do not are different in kind.**
    - **MUTATION 1 — not caught, and correctly so.** `renderOutreach()` gates the anchor on `model.ready && model.url` (`src/outreach-view.js:465`), so a populated `url` cannot reach the DOM while `ready` is false. The check asks the stronger question deliberately — its own prose: *"not 'the model says blocked' but 'there is no link'"* — and a model-level change does not move it. The `ready ?` is belt to the render's braces. **No repair; the design is right and the proof round says so.**
    - **MUTATION 2 — FAIL ×1**, the round-trip check. Non-vacuous.
    - **MUTATION 3 — FAIL ×1**, but not the check that was expected: the Acceptance-line-7 check calls `templatesFor()` directly in `evalJs` and so is blind to a change in the view, while the **on-screen picker** check caught it. Non-vacuous, by a different asker — which is the pair of surfaces this section was built to have.
    - **MUTATION 4 — FAIL ×2**, both presentation-mode checks. Non-vacuous.
    - **MUTATION 5 — NOT CAUGHT, and this one was a real defect in the delivered harness.** `update()` only SCHEDULES a save; `rev` advances inside `save()`, `DEBOUNCE_MS` later. `DEBOUNCE_MS` is **800ms** and the check read `rev` immediately after the flow, so **a write in the final 800ms was invisible to it** — and `toggleOutreachCopy` is the last control the flow touches. A check whose stated claim is *"the whole flow wrote nothing at all"* sat green over a planted writer.
  - **Repaired**: `tools/verify/outreach.mjs` now `await`s `window.planbook.store.flush()` before reading `rev` — deterministic rather than a sleep, and the idiom `verify/assignments.mjs:839` already uses. `flush()` clears the timers and awaits `save()`, which returns early when nothing is dirty, so a clean document still reads exactly what the check claims. **Re-proved both ways**: clean `1251 checks · 1251 passed · 0 failed · 0 skipped`, 409s, exit 0; and red under MUTATION 5, which it was not before.
  - **The line figure moved 37,455 → 37,466** (the eleven lines are that comment) and `tools/README.md` is corrected, including the claim that the old number was "measured on the delivered tree" — it was measured *before* the mutations, on a tree that then stopped existing.
  - **Worth carrying out of this**: the delivered tree's docs already leaned on the claim that check failed to prove — `TESTING.md` § WO-5.3 and the phase file both say `rev` does not move across the flow. It was **true but unproven**, and only the round the dead dispatch owed could tell the difference.
- 2026-08-28 — **verifier dispatched alone** (the implementer's work was already recovered), handed the recovery record with instructions to re-check it rather than trust it. **Verdict: PASS WITH MANUAL CHECKS — no Acceptance line failed.** It ran both tools itself and reproduced the **repaired** figures, not the corpse's: `1251 · 1251 · 0 · 0`, **37,466 lines, 409s**, 28 PASS / 0 FAIL / 0 SKIP in the send-flow section; sweep `34 · 31 · 0 · 3`; self-check 27/27; audit PASS.
  - It re-ran `grep -rn MUTATION` independently — nine hits, **all prose, none in `src/outreach.js` or `src/outreach-view.js`** — and judged all five reverts semantically right, including the high-risk one: **MUTATION 2's rebuilt CRLF normalisation checks out against RFC 6068 § 5**, with `\r\n` first in the alternation so an existing CRLF is not doubled, and pinned by three independent arms of the round-trip assertion. It also checked the *other* end of the `flush()` repair, which a half-fix would have missed: the `before` snapshot is itself taken after a flush, so both ends of the comparison are settled state.
  - Out of scope held: no `contact` entry anywhere, `src/merge-fields.js` untouched in the diff, `src/outreach-view.js`'s only store import is `getDoc` and its three `.push()` calls are into local arrays.
- 2026-08-28 — **three bookkeeping defects the dead dispatch left unpaid, all repaired this sitting.**
  - **B1 — the Status field still said a dispatch was in flight.** `🤖 CLAIMED` while every other statement about the row said 🔨. **The damage was live**: `wo-gate.mjs WO-5.3` told the next session to ask before proceeding, and `WO-5.4` refused with *"dependency is 🤖 CLAIMED"* rather than the true 👤 reason. Set to `🔨 IN PROGRESS — built 2026-08-28`. **`--release` would have been the wrong repair** — it writes ⬜ NOT STARTED and would have erased a built row. Re-checked: WO-5.4 now refuses on `🔨 IN PROGRESS <-- not done`, which is correct, because **👤 does not report code-complete the way 📆 does**.
  - **B2 — an off-by-one check count.** `phase-5-outreach.md` and `ROADMAP.md` both said `verify/outreach.mjs` is "twenty-seven checks"; it has **28** call sites, which is what `tools/README.md` and the sweep's own count claim already carried. Both corrected. (`TESTING.md` was already right at "twenty-eight sites".)
  - **B3 — `TESTING.md` still carried the corpse's figures** — 37,455 lines, 410s, "green on the delivered tree" — the same wording `tools/README.md` was corrected for, in the file that was not. Corrected to 37,466 / 409s, and it now says **why** it reads "recovered" rather than leaving the word unexplained.
  - Tools re-run after all four doc edits: sweep `34 · 31 · 0 · 3`, audit PASS.
- 2026-08-28 — **left uncommitted and unticked on purpose.** 18 staged paths. The two open Acceptance lines are 👤 and want an iPad; pushing to `main` is a production deploy here, so the commit is the owner's call.
- 2026-08-28 — **not repaired, flagged instead** (pre-existing, and a scheduling fact rather than a typo): `plans/work-orders/README.md` row 23 lists WO-5.4 as size **M**, its own header says **S**. Untouched by this work order.

## 2026-08-29 — the owner's reading, and what it produced

- **WO-5.3 is ✅ DONE — 2026-08-29.** The owner took both 👤 lines on hardware: the draft opened in the default mail client on the laptop and the iPad with subject and body intact, and the copy-to-self arrived in the sent folder. `--tick` applied; README dashboard 3/8 in Phase 5, total **124/152**; the four ROADMAP boxes (`Audience picker`, `Copy to self`, `mailto:` handoff, `Editable before sending`) ticked by hand and Phase 5 moved **4/9 → 8/9**, overall **60/81 → 64/81**. `--tick`'s recompute also corrected a pre-existing off-by-one in the README total (rows summed 123, the line said 122).
- **One defect, fixed the same day, and it belongs to WO-5.2 rather than to WO-5.3.** *"There's no way out of the message template screen."* `#templatesView` is the only full view that is not a `CLASS_SCREENS` entry, so the header's class-tab strip did not draw over it — and the caption branch drew a **dead `<span>Your classes</span>` over a panel headed *Message templates***, which is the calendar's own WO-6.6 bug arriving again in the sitting `src/classes.js` had warned would produce it. **The repair is not the one that comment predicted**: templates are global, so the editor takes the TABS without the KIND (`carriesTabs`), stays out of `CLASS_SCREENS`, and marks **no tab active** because it is in no class. `sw.js` CACHE **v103 → v104** — the owner's iPad was already holding v103, so without the bump the device would never have seen the fix. `verify-shell.mjs` green afterwards: `1251 · 1251 · 0 · 0`, 419s, exit 0.
- **The other six reports are booked, not folded in** — WO-5.5 through WO-5.8, written into the phase file with rows 38–41 in the running order. The split puts **WO-5.6 first**: it is the only one that stops the app destroying work a teacher typed, and **WO-5.8's gate refuses until it is done**, which is deliberate — several recipients change what "changing the recipient" means, so the confirm gets built once. **WO-5.8 reverses WO-5.3's seventh Acceptance line** (templates filtered on tone AND audience → tone only); that is the owner's call, is flagged in the work order and in the running-order row, and the line is to be amended in place rather than left contradicting a second document.
- Maintenance protocol paid in full: ROADMAP boxes + dashboard + the stale note that said the four boxes were "deliberately open"; `plans/work-orders/README.md` dashboard, § The files (**caught by `--audit`, not by me**), running order and Phase 5 status text; the phase file; `TESTING.md` readings; `CHANGELOG.md`; and `CLAUDE.md` — two blocks, the dead dispatch's scar and the third-view-kind ruling. `AGENTS.md` untouched: no rule changed, only status and scars.
- Tools at close: sweep `34 · 31 · 0 · 3`, audit PASS, self-check 27/27, harness `1251 · 1251 · 0 · 0`.
